<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUINotes.Views.NotePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUINotes"
    xmlns:models="using:WinUINotes.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<TextBox x:Name="NoteEditor"
                     Text="{x:Bind noteModel.Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     PlaceholderText="请输入文本"
                     Header=""
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>

			<WebView2 x:Name="MarkdownPreview"
                      Grid.Column="1"
                      DefaultBackgroundColor="Transparent"
                      Visibility="{x:Bind models:AppSettings.Instance.EnableMarkdownPreview, Mode=OneWay}"/>
		</Grid>

		<StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="4"
                    Grid.Row="1">
			<ToggleSwitch Header="Markdown预览" OffContent="关闭" OnContent="开启"
                         IsOn="{x:Bind models:AppSettings.Instance.EnableMarkdownPreview, Mode=TwoWay}"
                         Margin="0,0,16,0"/>
			<Button Content="保存" Click="SaveButton_Click" Style="{StaticResource AccentButtonStyle}"/>
			<Button Content="删除" Click="DeleteButton_Click"/>
		</StackPanel>
	</Grid>
</Page>
