<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUINotes.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUINotes.Views"
    xmlns:constants="using:WinUINotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <StackPanel Spacing="24" MaxWidth="600">
            <TextBlock Text="设置（并未完全做好）" Style="{ThemeResource TitleTextBlockStyle}"/>

            <StackPanel Spacing="12">
                <TextBlock Text="应用设置" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                <ToggleSwitch Header="深色模式" OffContent="关闭" OnContent="开启"/>

                <ToggleSwitch Header="自动保存" OffContent="关闭" OnContent="开启" IsOn="True"/>

                <NumberBox Header="自动保存间隔（秒）" Value="30" Minimum="5" Maximum="300"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="编辑器设置" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                <ToggleSwitch Header="启用Markdown预览" OffContent="关闭" OnContent="开启"/>

                <ComboBox Header="字体大小" SelectedIndex="1">
                    <ComboBoxItem Content="小"/>
                    <ComboBoxItem Content="中"/>
                    <ComboBoxItem Content="大"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="关于" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                <TextBlock Text="{x:Bind constants:Constants.AppName}" Style="{ThemeResource SubtitleTextBlockStyle}" FontSize="14"/>
                <TextBlock x:Name="VersionTextBlock" Style="{ThemeResource BodyTextBlockStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
